# My nvim config

## Installation
#### 1. Install nvim
```
brew install nvim
```

#### 2. Create a symbolic link of the nvim folder to ~/.config/nvim
```
ln -s "$(pwd)" ~/.config/nvim
```

#### 3. Start nvim
```
nvim
```
